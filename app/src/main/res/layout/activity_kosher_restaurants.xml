<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/ThemeOverlay.MaterialComponents"
    android:background="@drawable/bg_purple_gradient">

    <!-- Back -->
    <TextView
        android:id="@+id/restaurants_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="‹ Back"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Title -->
    <TextView
        android:id="@+id/kosher_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Kosher Restaurants"
        android:textColor="@color/black"
        android:textSize="22sp"
        android:fontFamily="@font/poppins_semibold"
        android:padding="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/restaurants_back" />

    <!-- Filters: round chips, no dialogs -->
    <com.google.android.material.chip.ChipGroup
        android:id="@+id/filter_group"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginTop="6dp"
        app:singleSelection="false"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/kosher_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Price toggles Any → € → €€ → €€€ → €€€€ -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chip_price"
            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Price: Any" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_shabbat"

            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Shabbat"
            app:checkedIconVisible="true" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_meat"

            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Meat" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_dairy"

            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dairy" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_pareve"

            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pareve" />
    </com.google.android.material.chip.ChipGroup>

    <!-- List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/restaurants_recycler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/filter_group"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty state -->
    <LinearLayout
        android:id="@+id/empty_states_no_restaurants"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/filter_group"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@android:drawable/ic_menu_report_image"
            android:alpha="0.85" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="No restaurants here yet"
            android:textSize="18sp"
            android:textColor="@android:color/black" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Try another country or check back later"
            android:textSize="14sp"
            android:textColor="#7A7A7A" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
